import React from 'react'
import CreateForm from "@/components/CreateForm";

// This is the page for creating a new product
const Page = () => {
    return (
        <CreateForm/>
    )
}
export default Page
